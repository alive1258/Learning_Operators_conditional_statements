#include <stdio.h>

int main(){
    int a =10;
    int b=2;
    int sum =a+b;
    printf("sumation of %d and %d is %d\n",a,b,sum);
    int sub= a-b;
    printf("subtraction of %d and %d is %d\n",a,b,sub);

    int mul =a* b;
    printf("multiplication of %d and %d is %d\n",a,b,mul);
    int div = a / b;
    printf("division of  is %d\n",div);
    return 0;
}